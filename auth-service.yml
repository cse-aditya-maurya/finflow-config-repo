server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/finflow_auth
    username: root
    password: Adi@2552

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

jwt:
  secret: finflow-secret-key-finflow-secret-key
  expiration: 86400000
