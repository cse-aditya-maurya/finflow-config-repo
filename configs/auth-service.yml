server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/finflow_auth
    username: root
    password: Adi@2552
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

jwt:
  secret: finflow-super-secret-jwt-key-must-be-at-least-256-bits-long-for-hs256
  expiration: 86400000

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
